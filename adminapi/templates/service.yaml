apiVersion: v1
kind: Service
metadata:
  name: {{ include "adminapi.fullname" . }}
  labels:
    {{- include "adminapi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "adminapi.selectorLabels" . | nindent 4 }}
