apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "adminapi.fullname" . }}-test-connection"
  labels:
    {{- include "adminapi.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "adminapi.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
